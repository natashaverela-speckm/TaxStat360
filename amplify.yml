version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - npm --version
        - nvm install 18
        - nvm use 18
        - node --version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
